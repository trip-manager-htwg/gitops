---
# Source: users-postgres/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: users-postgres
  namespace: trip-manager-prod
spec:
  type: ClusterIP
  selector:
    app: users-postgres
  ports:
    - port: 5432
      targetPort: 5432
---
# Source: users-postgres/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: users-postgres-headless
  namespace: trip-manager-prod
spec:
  clusterIP: None
  selector:
    app: users-postgres
  ports:
    - port: 5432
      targetPort: 5432
